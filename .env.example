# .env.example — committed to git as a template
# Copy to .env.production (never commit that file) and fill in real values.
# In CI/CD the content of .env.production is stored as the GitHub Secret ENV_PRODUCTION.

# ── Example variables (add yours below) ──────────────────────────
# VITE_API_URL=https://api.example.com
# VITE_CONTACT_EMAIL=hello@example.com
# VITE_ANALYTICS_ID=G-XXXXXXXXXX
